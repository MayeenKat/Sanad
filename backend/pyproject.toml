[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "BLE"]
ignore = ["B008", "E501"]
extend-per-file-ignores = { "tests/*" = ["BLE001"] }

[tool.pytest.ini_options]
testpaths = ["tests"]
